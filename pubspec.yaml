name: immoizi_core
description: "Shared models, API client, theme and widgets for the Immoizi mobile apps."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.2
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_secure_storage: ^9.2.4
  http: ^1.2.2
  shared_preferences: ^2.5.3
  video_player: ^2.9.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
